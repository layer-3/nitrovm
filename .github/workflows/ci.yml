name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    name: Go Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-opt
        run: |
          curl -fsSL https://github.com/WebAssembly/binaryen/releases/download/version_121/binaryen-version_121-x86_64-linux.tar.gz \
            | sudo tar -xz -C /usr/local --strip-components=1

      - name: Cache Rust build
        uses: actions/cache@v4
        with:
          path: |
            contracts/token/target
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('contracts/token/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build WASM contract
        run: make build-contract

      - name: Run tests
        run: CGO_ENABLED=1 go test -v -count=1 ./...
